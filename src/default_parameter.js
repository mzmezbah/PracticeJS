console.log(`lecture-six=== default-parameter: `)


In js, when we can pass an argument or parameter & if we set the parameter value constant then it called default parameter..bt when we does not declare the parameter then it took the default value.but if we declared the value then it find the new value...like under ,,


const sum = (a,b=1)=> a+b

// console.log(sum(10));
// console.log(sum(24,45));
